<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stayfit"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.stayfit.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="nosensor" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All Trainings Activity -->
        <activity
            android:name="com.example.stayfit.AllTrainigsActivity"
            android:label="@string/title_activity_all_trainigs" >
        </activity>

        <!-- New Training Activity -->
        <activity
            android:name="com.example.stayfit.NewTrainigActivity"
            android:label="Add New Training" >
        </activity>

        <!-- Training Details Activity -->
        <activity
            android:name="com.example.stayfit.TrainigDetailsActivity"
            android:label="Show Training Details" >
        </activity>
        <activity
            android:name="com.example.stayfit.TestActivity"
            android:label="@string/title_activity_test" >
        </activity>
        <activity
            android:name="com.example.stayfit.InformationActivity"
            android:label="@string/title_activity_information" >
        </activity>
    </application>

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
